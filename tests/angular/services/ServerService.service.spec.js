ngDescribe({
    name: 'Test ServerServiceService',
    modules: 'app',
    inject: 'ServerServiceService',
    tests: function (deps) {

        /*it('basic test', () => {
            //
        });*/
    }
});
